<!DOCTYPE html>
<!-- This is the main "Layout" and entry point of the app. The main "view"  This is what we see when we access the app -Greg -->
<!-- This is the main "Layout" and entry point of the app. The main "view"  This is what we see when we access the app -Greg -->
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
  </head>
  <body>
<!-- This is a partial found in the partials folder-->
<!-- name is what express recognizes with bodyparser-->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">Team 14</a>
    &nbsp;
    &nbsp;
    <form class="form-inline" method="post" action="search">
            <div class="form-row">
                <select class="custom-select" id="dropdown" name="filter">
                    <option selected>All</option>
                    <option value="E">Electronics</option>
                    <option value="B">Books</option>
                    <option value="F">Furniture</option>
                    <option value="O">Other</option>
                </select>
            </div>
          <input name="search" type="text" class="form-control" id="search" placeholder="Find Items" style="margin-left: 5.5px">
        <button type="submit" class="btn btn-info" style="margin-left: 1.5px">Search</button>
    </form>
    &nbsp;
    &nbsp;
    {{#if user}}
        <a class = "navbar-nav ml-auto" href="/users/logout">Logout</a>
    {{else}}
        <a class = "navbar-nav ml-auto" href="/users/login">Login/Register</a>
    {{/if}}
</nav>
<!-- body will be replaced by the various .hbs files that are in the views folder it starts with index.hbs and when you go to a new route eg. /route
     it will replace body with that .hbs file. This is basically a single page app.
-->
  {{{body}}}
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
  </body>
</html>
